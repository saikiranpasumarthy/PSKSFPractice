public class contactexists 
{
	public void oldcontact(map<id, contact> oldcons)
    {
        contact [] oldc = [select lastname from contact];
        for(contact old: oldcons.values())
        {
            
        }
    }
}